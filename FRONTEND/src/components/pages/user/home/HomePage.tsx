import { Box, Typography } from "@mui/material";

import React from "react";

type HomePageProps = {};

export default function HomePage(props: HomePageProps) {
	return (
		<Box>
			<Typography>Kezdőoldal</Typography>
		</Box>
	);
}
